{
  "name": "@acme/base",
  "version": "1.0.0",
  "description": "Base models and types for Acme platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "find dist -name '*.test.js' -exec node --test {} +"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
