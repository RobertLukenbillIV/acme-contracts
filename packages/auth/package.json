{
  "name": "@acme/auth",
  "version": "1.0.0",
  "description": "Authentication schemas for Acme platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "test": "find dist -name '*.test.js' -exec node --test {} +"
  },
  "dependencies": {
    "@acme/base": "1.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5.3.2"
  }
}
